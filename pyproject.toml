[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "GFC"
version = "1.0"
dependencies = [
  "Flask",
  "reportlab",
  "Werkzeug",
  "mysql-connector-python",
  "pandas",
  "easygui",
  "matplotlib",
  "openpyxl",
]
